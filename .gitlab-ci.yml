# You can override the included template(s) by including variable overrides
# SAST customization: https://docs.gitlab.com/ee/user/application_security/sast/#customizing-the-sast-settings
# Secret Detection customization: https://docs.gitlab.com/ee/user/application_security/secret_detection/pipeline/#customization
# Dependency Scanning customization: https://docs.gitlab.com/ee/user/application_security/dependency_scanning/#customizing-the-dependency-scanning-settings
# Container Scanning customization: https://docs.gitlab.com/ee/user/application_security/container_scanning/#customizing-the-container-scanning-settings
# Note that environment variables can be set in several places
# See https://docs.gitlab.com/ee/ci/variables/#cicd-variable-precedence

variables:
  AWS_REGION: us-east-2
  AGENTCORE_ENTRYPOINT: deploy/agentcore/a2a_server.py
  AGENTCORE_REQUIREMENTS: deploy/agentcore/requirements.txt

stages:
- build
- test
- deploy
- agentcore
- target-app-deploy
- review
- dast
- staging
- canary
- production
- incremental rollout 10%
- incremental rollout 25%
- incremental rollout 50%
- incremental rollout 100%
- performance
- cleanup
sast:
  stage: test

agentcore:deploy:
  stage: agentcore
  image: python:3.12-slim
  when: manual
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
  before_script:
    - pip install --no-cache-dir bedrock-agentcore-starter-toolkit
    - pip install --no-cache-dir -r deploy/agentcore/requirements.txt
  script:
    - |
      agentcore configure \
        --entrypoint "$AGENTCORE_ENTRYPOINT" \
        --requirements-file "$AGENTCORE_REQUIREMENTS" \
        --protocol A2A \
        --deployment-type container \
        --name "${AGENTCORE_AGENT_NAME}" \
        --region "$AWS_REGION" \
        --disable-memory \
        --non-interactive
    - |
      agentcore deploy --agent "${AGENTCORE_AGENT_NAME}" \
        --env "AGENTCORE_AGENT=${AGENTCORE_AGENT_NAME}" \
        --env "AWS_REGION=${AWS_REGION}" \
        --env "ARTIFACT_STORE=s3" \
        --env "ARTIFACT_S3_BUCKET=${ARTIFACT_S3_BUCKET}" \
        --env "ARTIFACT_DYNAMODB_TABLE=${ARTIFACT_DYNAMODB_TABLE}"
  variables:
    AGENTCORE_AGENT_NAME: architect-agent

# Phase 2 (devops-agent GitLab integration, standalone from the AgentCore chain):
# fires only on branches gitlab-agent publishes (sdlc/<app>).
#
# TEMPORARY: `when: manual` removed for a showcase run (auto-fires on push to
# sdlc/*). This runs a real terraform apply against AWS with no human gate —
# switch back to `when: manual` before this is used for anything beyond a demo.
#
# The image/services/before_script below install everything the job needs
# (pwsh, Docker CLI, Terraform, Python deps) so it doesn't depend on a
# pre-baked runner image. What's still a runner/project setting, not
# something this file can fix:
#   - The runner must allow privileged mode for the docker:dind service
#     (GitLab Runner config, not repo config) — without it, "docker info"
#     below fails fast with a clear message instead of hanging deep in terraform.
#   - AWS credentials as protected/masked CI/CD variables (Settings > CI/CD >
#     Variables): AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, AWS_DEFAULT_REGION
#     (or swap for OIDC role assumption). SSO login (`aws sso login`) is
#     interactive and can't run in CI, so local credential setup doesn't
#     carry over — checked below before anything else runs.
target-app:deploy:
  stage: target-app-deploy
  image: mcr.microsoft.com/powershell:7.4-debian-12
  services:
    - docker:24-dind
  variables:
    DOCKER_HOST: tcp://docker:2375
    DOCKER_TLS_CERTDIR: ""
    TF_VERSION: "1.10.5"
  rules:
    - if: '$CI_COMMIT_BRANCH =~ /^sdlc\//'
  before_script:
    - export SLUG="${CI_COMMIT_BRANCH#sdlc/}"
    - 'echo "Target app slug: $SLUG"'
    - |
      for v in AWS_ACCESS_KEY_ID AWS_SECRET_ACCESS_KEY AWS_DEFAULT_REGION; do
        if [ -z "$(printenv "$v")" ]; then
          echo "Missing required CI/CD variable: $v — add it under Settings > CI/CD > Variables (protected+masked). See comment above this job."
          exit 1
        fi
      done
    - apt-get update -qq && apt-get install -y -qq curl unzip python3 python3-pip docker.io git
    - curl -fsSL "https://releases.hashicorp.com/terraform/${TF_VERSION}/terraform_${TF_VERSION}_linux_amd64.zip" -o /tmp/tf.zip
    - unzip -q /tmp/tf.zip -d /usr/local/bin && rm /tmp/tf.zip
    - terraform -version
    - docker info > /dev/null || (echo "Docker daemon not reachable — the runner likely doesn't allow privileged mode for docker:dind. Ask whoever manages the runner to enable it." && exit 1)
    - pip3 install --no-cache-dir --break-system-packages -r backend/requirements.txt
  script:
    - cd backend
    - python3 agents/devops-agent/devops_agent.py --target-app "$SLUG" --deploy

include:
- template: Auto-DevOps.gitlab-ci.yml