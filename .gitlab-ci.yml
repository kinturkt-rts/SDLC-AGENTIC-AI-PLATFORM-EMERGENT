# You can override the included template(s) by including variable overrides
# SAST customization: https://docs.gitlab.com/ee/user/application_security/sast/#customizing-the-sast-settings
# Secret Detection customization: https://docs.gitlab.com/ee/user/application_security/secret_detection/pipeline/#customization
# Dependency Scanning customization: https://docs.gitlab.com/ee/user/application_security/dependency_scanning/#customizing-the-dependency-scanning-settings
# Container Scanning customization: https://docs.gitlab.com/ee/user/application_security/container_scanning/#customizing-the-container-scanning-settings
# Note that environment variables can be set in several places
# See https://docs.gitlab.com/ee/ci/variables/#cicd-variable-precedence

variables:
  AWS_REGION: us-east-2
  AGENTCORE_ENTRYPOINT: deploy/agentcore/a2a_server.py
  AGENTCORE_REQUIREMENTS: deploy/agentcore/requirements.txt

stages:
- build
- test
- deploy
- agentcore
- target-app-deploy
- review
- dast
- staging
- canary
- production
- incremental rollout 10%
- incremental rollout 25%
- incremental rollout 50%
- incremental rollout 100%
- performance
- cleanup
sast:
  stage: test

agentcore:deploy:
  stage: agentcore
  image: python:3.12-slim
  when: manual
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
  before_script:
    - pip install --no-cache-dir bedrock-agentcore-starter-toolkit
    - pip install --no-cache-dir -r deploy/agentcore/requirements.txt
  script:
    - |
      agentcore configure \
        --entrypoint "$AGENTCORE_ENTRYPOINT" \
        --requirements-file "$AGENTCORE_REQUIREMENTS" \
        --protocol A2A \
        --deployment-type container \
        --name "${AGENTCORE_AGENT_NAME}" \
        --region "$AWS_REGION" \
        --disable-memory \
        --non-interactive
    - |
      agentcore deploy --agent "${AGENTCORE_AGENT_NAME}" \
        --env "AGENTCORE_AGENT=${AGENTCORE_AGENT_NAME}" \
        --env "AWS_REGION=${AWS_REGION}" \
        --env "ARTIFACT_STORE=s3" \
        --env "ARTIFACT_S3_BUCKET=${ARTIFACT_S3_BUCKET}" \
        --env "ARTIFACT_DYNAMODB_TABLE=${ARTIFACT_DYNAMODB_TABLE}"
  variables:
    AGENTCORE_AGENT_NAME: architect-agent

# Phase 2 (devops-agent GitLab integration, standalone from the AgentCore chain):
# fires only on branches gitlab-agent publishes (sdlc/<app>).
#
# TEMPORARY: `when: manual` removed for a showcase run (auto-fires on push to
# sdlc/*). This runs a real terraform apply against AWS with no human gate —
# switch back to `when: manual` before this is used for anything beyond a demo.
#
# Prerequisites not yet configured (job will fail until these are set up):
#   - Runner needs Docker (build+push to ECR) and Terraform on PATH.
#   - AWS credentials as protected/masked CI/CD variables (or OIDC role
#     assumption) — SSO login (`aws sso login`) is interactive and won't work
#     in CI, so this needs a different credential path than local runs use.
#   - devops_agent.py's run_deploy() hardcodes the "powershell" binary name
#     (agents/devops-agent/devops_agent.py:451), which only exists on Windows.
#     A Linux runner only has "pwsh" if PowerShell Core is installed. This
#     needs a decision before the job can succeed: either target a Windows
#     runner, or make run_deploy() pick the binary by platform.
target-app:deploy:
  stage: target-app-deploy
  image: python:3.12-slim
  rules:
    - if: '$CI_COMMIT_BRANCH =~ /^sdlc\//'
  before_script:
    - export SLUG="${CI_COMMIT_BRANCH#sdlc/}"
    - echo "Target app slug: $SLUG"
    - pip install --no-cache-dir -r backend/requirements.txt
  script:
    - cd backend
    - python agents/devops-agent/devops_agent.py --target-app "$SLUG" --deploy

include:
- template: Auto-DevOps.gitlab-ci.yml