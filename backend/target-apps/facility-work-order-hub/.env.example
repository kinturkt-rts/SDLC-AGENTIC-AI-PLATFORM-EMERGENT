# App
APP_ENV=development
SERVICE_NAME=facility-work-order-hub

# Database (PostgreSQL RDS)
# Every line needs the variable name — paste DATABASE_URL=postgresql+psycopg://..., not a bare URL.
DATABASE_URL=postgresql+psycopg://user:password@agenticaidbinstance.c1u0cggiolxp.us-east-2.rds.amazonaws.com:5432/sdlc_agentic_ai?sslmode=require
POSTGRES_SCHEMA=facility_work_order_hub

# JWT Auth
JWT_SECRET_KEY=change-me-to-a-random-secret
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=60

# CORS
CORS_ORIGINS=["*"]
