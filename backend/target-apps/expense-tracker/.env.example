# Expense Tracker — Environment Variables
# Copy this file to .env and fill in your real values.
# Every line must be KEY=value — never paste a bare URL without the variable name.

APP_ENV=development
SERVICE_NAME=expense-tracker
DATABASE_URL=postgresql+psycopg://postgres:yourpassword@agenticaidbinstance.c1u0cggiolxp.us-east-2.rds.amazonaws.com:5432/sdlc_agentic_ai?sslmode=require
POSTGRES_SCHEMA=expense_tracker
API_KEY=ADMIN_KEY_DEV
CORS_ORIGINS=["*"]
AWS_REGION=us-east-2
LOG_LEVEL=INFO
